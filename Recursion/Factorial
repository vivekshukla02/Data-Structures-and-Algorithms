class Solution {
    long factorial(int ) {
        // code here
        if(n==0)return 1;
        
        long ans = factorial(n-1);
        long Nans = factorial(n-1)*n;
        return Nans;
    }
}
