class Solution {
    public int search(int arr[], int x) {
        // code here
        for(int idx=0;idx<arr.length;idx++){
            if(arr[idx]==x){
                return idx;
            }
        }
       
        
    }
}
